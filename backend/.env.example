# =============================================================================
# INFISICAL - Secret Management (eu.infisical.com)
# =============================================================================
# The only secret stored locally. All other secrets are fetched from Infisical.
# Create a Service Token in: Project Settings > Service Tokens (Read, Development)
INFISICAL_TOKEN=st.your_service_token_here

# =============================================================================
# LOCAL FALLBACK (only used if Infisical is unavailable / for local dev without Infisical)
# =============================================================================

# Server
PORT=5000
NODE_ENV=development

# Database
MONGODB_URI=mongodb+srv://<user>:<password>@cluster.mongodb.net/ai-aggregator

# JWT
JWT_SECRET=your_jwt_secret_key_here

# AI API Keys
GOOGLE_GEMINI_API_KEY=your_gemini_api_key_here
MISTRAL_API_KEY=your_mistral_api_key_here
HUGGINGFACE_API_KEY=your_huggingface_token_here
COHERE_API_KEY=your_cohere_api_key_here

# CORS
FRONTEND_URL=http://localhost:5173
